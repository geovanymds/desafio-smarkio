const messageRouter = require('./message');
const userRouter = require('./user');
const intentionRouter = require('./intention');

module.exports = {
  messageRouter,
  userRouter,
  intentionRouter
};